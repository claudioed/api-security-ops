apiVersion: v1
kind: Service
metadata:
  labels:
    app: legacy
    stack: echo
    interface: rest
    language: golang
  name: legacy
spec:
  ports:
    - name: http
      port: 9999
      protocol: TCP
      targetPort: 9999
  selector:
    app: legacy